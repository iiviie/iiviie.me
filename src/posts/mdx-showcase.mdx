---
title: "MDX Typography Showcase"
description: "A comprehensive guide showcasing all the different text formatting options available in MDX blog posts"
date: "January 15, 2025"
tags: ["demo", "mdx", "typography"]
---

# Main Heading (H1)

This is a paragraph with regular body text. The quick brown fox jumps over the lazy dog. This sentence contains enough characters to demonstrate how paragraphs flow and wrap on different screen sizes.

You can write multiple paragraphs, and they'll have appropriate spacing between them. This makes long-form content much easier to read and digest.

## Section Heading (H2)

This is how a major section heading looks. Use H2 for main sections of your blog post.

### Subsection Heading (H3)

This is a subsection under the main section. Use H3 for subsections.

#### Minor Heading (H4)

This is the smallest heading level. Use it for minor sections or notes.

## Text Formatting

You can use **bold text** for emphasis, *italic text* for subtle emphasis, and `inline code` for technical terms or commands.

Here's a sentence with all three: The `npm install` command will **install** all dependencies *automatically*.

## Links

External links look like this: [Visit OpenCode](https://opencode.ai)

Internal links work too: [Back to home](/)

You can also link to other blog posts: [Read another post](/blog/hello-world)

## Code Blocks

Here's a JavaScript code block with syntax:

```javascript
function greet(name) {
  console.log(`Hello, ${name}!`);
  return `Welcome to the blog`;
}

const message = greet('World');
```

Python code example:

```python
def calculate_fibonacci(n):
    if n <= 1:
        return n
    return calculate_fibonacci(n-1) + calculate_fibonacci(n-2)

# Calculate the 10th Fibonacci number
result = calculate_fibonacci(10)
print(f"Result: {result}")
```

Terminal commands:

```bash
npm run dev
npm run build
npm run lint
```

## Lists

### Unordered Lists

Here's what you need to know:

- First item in the list
- Second item with more detail
- Third item
  - Nested item one
  - Nested item two
- Fourth item back at the top level

### Ordered Lists

Step-by-step instructions:

1. Clone the repository
2. Install dependencies with `npm install`
3. Start the development server
4. Open your browser to localhost:3000
5. Begin coding!

## Blockquotes

> This is a blockquote. Use it for highlighting important information, quotes from other sources, or calling out key insights.
>
> Blockquotes can span multiple paragraphs and will maintain consistent styling throughout.

> "The best way to predict the future is to invent it." - Alan Kay

## Tables

Tables are perfect for structured data:

| Feature | Description | Status |
|---------|-------------|--------|
| Hover Effects | Cyan highlight on rows | ✅ Complete |
| Typography | Improved heading hierarchy | ✅ Complete |
| Code Blocks | Left accent bar | ✅ Complete |
| Mobile Support | Responsive design | ✅ Complete |

Here's a comparison table:

| Framework | Language | Performance | Learning Curve |
|-----------|----------|-------------|----------------|
| Next.js | JavaScript | Excellent | Medium |
| Django | Python | Very Good | Medium |
| FastAPI | Python | Excellent | Easy |
| Express | JavaScript | Good | Easy |

## Mixed Content Example

When building a **modern web application**, you often need to combine multiple technologies. Here's a typical stack:

### Backend Setup

1. Initialize your project:
```bash
mkdir my-project
cd my-project
npm init -y
```

2. Install dependencies:
```bash
npm install express mongoose dotenv
npm install -D nodemon
```

3. Create your main server file (`server.js`):
```javascript
const express = require('express');
const app = express();
const PORT = process.env.PORT || 3000;

app.get('/', (req, res) => {
  res.json({ message: 'API is running' });
});

app.listen(PORT, () => {
  console.log(`Server running on port ${PORT}`);
});
```

> **Pro Tip**: Always use environment variables for sensitive configuration like API keys and database URLs.

### Frontend Connection

Connect your frontend with these steps:

- Set up your API client
- Handle authentication tokens
- Implement error handling
- Add loading states

## Advanced Features

### Inline Code with Context

Use the `useState` hook for local state, `useEffect` for side effects, and `useContext` for global state management.

### Multiple Code Languages

SQL query example:
```sql
SELECT users.name, orders.total
FROM users
JOIN orders ON users.id = orders.user_id
WHERE orders.status = 'completed'
ORDER BY orders.total DESC
LIMIT 10;
```

JSON configuration:
```json
{
  "name": "my-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  }
}
```

## Images

You can embed images from UploadThing CDN:

![Example image from UploadThing](https://jxa4tmdd3o.ufs.sh/f/iEchx6H2lZSoO1mSOQ3iVcydtlHgfrw3GzU9kbExujQ4FeZS)

Images will be automatically:
- Responsive and optimized
- Displayed with rounded corners
- Shown with a caption (from alt text)

## Conclusion

This post demonstrates all the formatting options available in MDX:

- ✅ Headers (H1 through H4)
- ✅ Paragraphs with proper spacing
- ✅ Text formatting (bold, italic, inline code)
- ✅ Links (internal and external)
- ✅ Code blocks with syntax highlighting
- ✅ Ordered and unordered lists
- ✅ Blockquotes
- ✅ Tables
- ✅ Images from CDN
- ✅ Mixed content layouts

Check out the [source code](https://github.com) to see how these elements are implemented!
